<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Pinterest clone</title>
    </head>
    <body>
    <h1>Pins</h1>
    {% for pin in pins %}
        <article>
        <h1>{{pin.getTitle()}}</h1>
        <p>{{pin.getDescription()}}</p>
        </article>
    {% endfor %}
    
        
    </body>
</html>
